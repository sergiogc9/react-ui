import { ArgsTable, Meta, Story, Canvas } from 'storybook/utils/components';
import {
  getFixedStoryParams,
  getPlaygroundStoryParams
} from 'storybook/parameters';
import Title from 'components/Title';

<Meta title="Components/Title" component={Title} />

# Title

Title component.

<Story
  name="playground"
  args={{ children: 'Play with me', textAlign: 'center', fontFamily: 'main' }}
  parameters={getPlaygroundStoryParams()}
>
  {(args) => <Title {...args} />}
</Story>

## AspectSize prop

The prop `aspectSize` receives a `string` value.

Based on that value it changes the `font-weight, font-size and line-height` styles of the Content component.

<Canvas>
  <Story name="upper" parameters={getFixedStoryParams()}>
    <Title aspectSize="upper">I'm a upper title</Title>
  </Story>
  <Story name="subtle" parameters={getFixedStoryParams()}>
    <Title aspectSize="subtle">I'm a subtle title</Title>
  </Story>
  <Story name="xs" parameters={getFixedStoryParams()}>
    <Title aspectSize="xs">I'm a xs title</Title>
  </Story>
  <Story name="s" parameters={getFixedStoryParams()}>
    <Title aspectSize="s">I'm a s title</Title>
  </Story>
  <Story name="m" parameters={getFixedStoryParams()}>
    <Title aspectSize="m">I'm a m title</Title>
  </Story>
  <Story name="l" parameters={getFixedStoryParams()}>
    <Title aspectSize="l">I'm a l title</Title>
  </Story>
  <Story name="xl" parameters={getFixedStoryParams()}>
    <Title aspectSize="xl">I'm a xl title</Title>
  </Story>
</Canvas>

<ArgsTable of={Title} />
