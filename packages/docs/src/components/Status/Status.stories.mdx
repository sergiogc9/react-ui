import { ArgsTable, Meta, Story, Canvas } from 'storybook/utils/components';
import { getFixedStoryParams, getPlaygroundStoryParams } from 'storybook/parameters';
import Status from 'components/Status';

<Meta title="Components/Status" component={Status} />

# Status

Status component.

<Story
	argTypes={{ bg: { control: { type: 'text' } } }}
	args={{ variant: 'blue' }}
	name="playground"
	parameters={getPlaygroundStoryParams()}
>
	{args => <Status {...args} />}
</Story>

## Variants

Change the `Status` styling using the different availables variants changing the `variant` prop:

<Canvas>
	<Story name="grey" parameters={getFixedStoryParams()}>
		<Status variant="grey" numberOfItems={50} />
	</Story>
	<Story name="red" parameters={getFixedStoryParams()}>
		<Status variant="red" numberOfItems={50} />
	</Story>
	<Story name="blue" parameters={getFixedStoryParams()}>
		<Status variant="blue" numberOfItems={50} />
	</Story>
	<Story name="green" parameters={getFixedStoryParams()}>
		<Status variant="green" numberOfItems={50} />
	</Story>
	<Story name="yellow" parameters={getFixedStoryParams()}>
		<Status variant="yellow" numberOfItems={50} />
	</Story>
	<Story name="custom background" parameters={getFixedStoryParams()}>
		<Status numberOfItems={50} bg="#F0B27A" />
	</Story>
</Canvas>

# API

<ArgsTable of={Status} />
