import { ArgsTable, Meta, Story, Canvas } from 'storybook/utils/components';
import { getFixedStoryParams, getPlaygroundStoryParams } from 'storybook/parameters';
import Box from 'components/Box';

<Meta title="Components/Box" component={Box} />

# Box

Box component.

<Story name="playground" args={{ children: 'Play with me' }} parameters={getPlaygroundStoryParams()}>
	{args => <Box {...args} />}
</Story>

## Sizes

<Canvas>
	<Story name="default box" parameters={getFixedStoryParams()}>
		{() => <Box children="Awesome box!" />}
	</Story>
	<Story name="coloured box" parameters={getFixedStoryParams()}>
		{() => <Box color="blue.500" children="Awesome box!" />}
	</Story>
	<Story name="bordered box" parameters={getFixedStoryParams()}>
		{() => <Box border="solid thin blue" children="Awesome box!" />}
	</Story>
</Canvas>

<ArgsTable of={Box} />
