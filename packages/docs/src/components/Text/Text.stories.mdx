import { ArgsTable, Meta, Story, Canvas } from 'storybook/utils/components';
import { getFixedStoryParams, getPlaygroundStoryParams } from 'storybook/parameters';

import Text from 'components/Text';

<Meta title="Components/Text" component={Text} />

# Text

Text component.

<Story
	name="playground"
	args={{ children: 'Play with me', textAlign: 'center', fontFamily: 'main' }}
	parameters={getPlaygroundStoryParams()}
>
	{args => <Text {...args} />}
</Story>

## AspectSize prop

The prop `aspectSize` receives a `string` value.

Based on that value it changes the `font-weight, font-size and line-height` styles of the Text component.

<Canvas>
	<Story name="xs" parameters={getFixedStoryParams()}>
		<Text aspectSize="xs">I'm a xs text</Text>
	</Story>
	<Story name="s" parameters={getFixedStoryParams()}>
		<Text aspectSize="s">I'm a s text</Text>
	</Story>
	<Story name="m" parameters={getFixedStoryParams()}>
		<Text aspectSize="m">I'm a m text</Text>
	</Story>
	<Story name="l" parameters={getFixedStoryParams()}>
		<Text aspectSize="l">I'm a l text</Text>
	</Story>
	<Story name="xl" parameters={getFixedStoryParams()}>
		<Text aspectSize="xl">I'm a xl text</Text>
	</Story>
</Canvas>

# API

<ArgsTable of={Text} />
