import { ArgsTable, Meta, Story, Canvas } from 'storybook/utils/components';
import { getFixedStoryParams, getPlaygroundStoryParams } from 'storybook/parameters';
import RippleDecorator from 'storybook/decorators/Ripple';
import Box from 'components/Box';
import Ripple from 'components/Ripple';

<Meta title="Components/Ripple" component={Ripple} decorators={[RippleDecorator]} />

# Ripple

Test it:

<Story
	name="playground"
	args={{ color: 'primary.500' }}
	parameters={getPlaygroundStoryParams({
		docs: { disable: false },
		include: ['color']
	})}
>
	{args => (
		<Box
			bg="currentColor"
			border="thin solid lightgray"
			borderRadius="50%"
			height="300px"
			overflow="hidden"
			position="relative"
			width="300px"
		>
			<Ripple {...args} />
		</Box>
	)}
</Story>

# API

<ArgsTable of={Ripple} include={['color']} />
