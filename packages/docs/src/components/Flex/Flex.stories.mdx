import { ArgsTable, Meta, Story, Canvas } from 'storybook/utils/components';
import { getFixedStoryParams, getPlaygroundStoryParams } from 'storybook/parameters';
import Flex from 'components/Flex';

<Meta title="Components/Flex" component={Flex} />

# Flex

Flex component. Same than Box but with `display: flex;` by default.

<Story name="playground" args={{ children: 'Play with me' }} parameters={getPlaygroundStoryParams()}>
	{args => <Flex {...args} />}
</Story>

## Examples

<Canvas>
	<Story name="default flex" parameters={getFixedStoryParams()}>
		{() => <Flex children="Awesome flex!" />}
	</Story>
	<Story name="coloured flex" parameters={getFixedStoryParams()}>
		{() => <Flex color="blue.500" children="Awesome flex!" />}
	</Story>
	<Story name="bordered flex" parameters={getFixedStoryParams()}>
		{() => <Flex border="solid thin blue" children="Awesome flex!" />}
	</Story>
</Canvas>

<ArgsTable of={Flex} />
