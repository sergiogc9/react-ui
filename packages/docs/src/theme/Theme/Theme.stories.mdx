import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import styled from 'styled-components';
import { Box } from '@sergiogc9/react-ui';
import theme from '@sergiogc9/react-ui-theme';

import BlurDecorator from 'storybook/decorators/Blur';
import ThemeDecorator from 'storybook/decorators/Theme';
import Theme from './Theme';

<Meta
  title="Theme/Theme"
  decorators={[ThemeDecorator]}
  argTypes={{
    primaryColor: {
      control: 'color',
      defaultValue: theme.colors.primary['500']
    },
    secondaryColor: {
      control: 'color',
      defaultValue: theme.colors.secondary
    }
  }}
/>

# Theme

Change colors and variables to see how these changes are reflected in UI kit components below:

🚧 In progress... See the components docs instead!
